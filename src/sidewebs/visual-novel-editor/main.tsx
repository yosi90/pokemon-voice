import { createRoot } from 'react-dom/client';
import { VisualNovelEditor } from './VisualNovelEditor.js';
import './styles.css';

createRoot(document.getElementById('root')!).render(<VisualNovelEditor />);
